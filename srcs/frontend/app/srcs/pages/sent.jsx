import ftReact from "../ft_react";
import BarLayout from "../components/barlayout";

const Sent = (props) => {
    return (
        <BarLayout route={props.route}>
            <h1>Sent</h1>
        </BarLayout>
    );
};

export default Sent;